{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "teachingSessionMetadata",
  "description": "Schema for storing metadata about a Teaching Session in Yogis",
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string",
      "description": "Unique identifier for this teaching session"
    },
    "userId": {
      "type": "string",
      "description": "Reference to the user (Firebase UID) running the session"
    },
    "planId": {
      "type": "string",
      "description": "Reference to the ClassPlan being taught"
    },
    "startedAt": {
      "type": "integer",
      "description": "Timestamp (ms since epoch) when the session began"
    },
    "endedAt": {
      "type": ["integer", "null"],
      "description": "Timestamp when the session ended (null until finished)"
    },
    "currentElementOrder": {
      "type": "integer",
      "description": "Index/order of the pose or flow the user is currently on (0‑based)"
    }
  },
  "required": [
    "sessionId",
    "userId",
    "planId",
    "startedAt",
    "endedAt",
    "currentElementOrder"
  ],
  "additionalProperties": false
}
